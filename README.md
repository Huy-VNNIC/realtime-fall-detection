# realtime-fall-detection
Realtime fall detection from webcam using OpenCV (no YOLO). State machine + timer to trigger alarms and send alerts to an iOS app via WebSocket/APNs. Includes Python service and iOS SwiftUI app.
